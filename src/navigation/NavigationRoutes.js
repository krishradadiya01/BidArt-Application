import Splash from '../Tab/Splash';
import OnBoarding from '../Tab/OnBoarding';
import AuthNavigation from './type/AuthNavigation';
import Login from '../container/Auth/Login';
import CreatePassword from '../container/Auth/CreatePassword';

export const StackRoute = {
  Splash,
  OnBoarding,
  AuthNavigation,
};

export const AuthRoute = {
  Login,
  CreatePassword,
};
